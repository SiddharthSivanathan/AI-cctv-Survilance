{
  "name": "@visionops/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for VisionOps AI. API DTOs will be generated from OpenAPI here.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[types] no tests yet\" && exit 0",
    "build": "tsc",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@visionops/config": "workspace:*",
    "eslint": "^9.11.1",
    "typescript": "^5.6.2"
  }
}
