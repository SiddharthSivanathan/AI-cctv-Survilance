{
  "name": "@visionops/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for VisionOps AI. API DTOs will be generated from OpenAPI here.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"[types] no eslint (type-checked via tsc build)\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"[types] no tests yet\" && exit 0",
    "build": "tsc",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "@visionops/config": "workspace:*",
    "eslint": "^10.6.0",
    "typescript": "^5.6.2"
  }
}
